<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <%= link_to "Home", main_dashboard_index_path, class: 'navbar-brand' %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if customer_signed_in? %>
          <li class="nav-item">
            <%= link_to "Products", products_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to "Orders", orders_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to "New Order", new_order_path, class: 'nav-link' %>
          </li>
        <% end %>
        <% unless customer_signed_in? %>
          <li class="nav-item">
            <%= link_to "Login", new_customer_session_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to "Signup", new_customer_registration_path, class: 'nav-link' %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Logout", destroy_customer_session_path, data: { turbo_method: :delete }, class: 'nav-link btn btn-danger' %>
          </li>
        <% end %>
      </ul>
      <div id="cart-button"><%= render 'layouts/cart' %></div>
    </div>
  </div>
</nav>